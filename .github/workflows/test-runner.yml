name: Test AI Factory Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Check server
        run: |
          echo "AI Factory Runner OK"
          hostname
          python3 --version
          node -v
          java -version
          gradle -v
